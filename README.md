# HungerBox UI

React frontend for the HungerBox application.

This project provides the user interface for:
- Viewing food items from multiple counters
- Placing food orders
- Viewing user-specific orders

The frontend consumes REST APIs exposed by the **HungerBox Backend**.

> Note: This repository contains only the frontend code.
